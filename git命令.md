# git命令

- 确认当前分支

```bash
git branch
```

- 切换分支

```bash
git checkout name
```

- 推送到远程分支

```bash
git add .
git commit -m "your commit message"
git push origin name
```

- 将分支里的更新合并到master

```bash
git checkout master
git merge firstPart
```

